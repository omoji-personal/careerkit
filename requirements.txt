# Runtime only. Test tooling lives in requirements-dev.txt so installing
# CareerKit to USE it does not pull a test framework.
PyYAML>=6.0
requests>=2.31

# Optional. Adds Indeed / ZipRecruiter / Glassdoor / Google via the `jobspy`
# feed. Everything else works without it; the feed reports itself as dormant
# when the import is unavailable. Uncomment to enable.
# python-jobspy>=1.1.80
