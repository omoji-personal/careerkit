# Reusable runtime dependency set. The supported clone setup installs
# requirements-dev.txt as well so every user can run the regression suite.
PyYAML>=6.0
packaging>=24.0
# 2.33.0 closes the redirect/netrc credential disclosure advisories affecting
# the formerly allowed 2.31.x/2.32.x line.
requests>=2.33.0
